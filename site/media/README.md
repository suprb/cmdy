# Demo source media

Full-length screen-recording masters live here during local editing and are
ignored by Git. Commit only the short, optimized AV1 and H.264 clips under
`site/public/`; this keeps the static site fast and the repository history
small.
